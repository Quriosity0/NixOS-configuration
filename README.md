# NixOS-configuration
my config files for NixOS Linux

I use these commands to update configuration

```
sudo nixos-rebuild switch --flake ~/nixconf#asuspc
home-manager switch --flake ~/nixconf#quriosity
sudo nix-collect-garbage -d
```
